{
  "name": "after-party",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/build-docs.ts",
    "report:codex": "node scripts/codex-session-report.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@azure/msal-browser": "5.17.1"
  },
  "devDependencies": {
    "@types/node": "24.10.4",
    "jsdom": "29.1.1",
    "marked": "18.0.7",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
