/**
 * The single seam for future use-case-specific Azure and Microsoft Graph calls.
 * It intentionally has no methods until the API has a real cloud operation.
 */
export interface CloudOperations {}
